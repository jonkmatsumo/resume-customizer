\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{hyperref}

% Page geometry: tight margins for one-page constraint
\geometry{
    left=0.5in,
    right=0.5in,
    top=0.5in,
    bottom=0.5in,
    marginparwidth=0pt,
    marginparsep=0pt
}

% Disable page numbering
\pagestyle{empty}

% Tighten spacing
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\itemsep}{0pt}
\setlength{\topsep}{0pt}
\setlength{\partopsep}{0pt}
\setlength{\parsep}{0pt}

% Custom bullet spacing
\setlist[itemize]{leftmargin=*,itemsep=2pt,parsep=0pt,topsep=2pt}

\begin{document}

% Header Section
\begin{center}
    {\huge\textbf{ {{- .Name -}} }}\\[0.3cm]
    {{- if .Email -}} \texttt{ {{- .Email -}} } {{- if .Phone }} | {{ .Phone }} {{- end -}} {{- else -}} {{- if .Phone -}} {{ .Phone }} {{- end -}} {{- end -}}
\end{center}

\vspace{0.2cm}

% Experience Section
\section*{Experience}

{{ range .Companies }}
{\large\textbf{ {{- .Company -}} }}
{{ range .Roles }}

\textit{ {{- .Role -}} } \hfill {{ .DateRanges }}

\begin{itemize}
{{ range .Bullets }}
    \item {{ . }}
{{ end }}
\end{itemize}
{{ end }}

\vspace{0.15cm}
{{ end }}

{{ if .Education }}
% Education Section
\section*{Education}

{{ range .Education }}
{\large\textbf{ {{- .School -}} }} \hfill {{ .DateRange }}

\textit{ {{- .Degree }} in {{ .Field -}} }{{ if .GPA }} | GPA: {{ .GPA }}{{ end }}

{{ if .Highlights }}
\begin{itemize}
{{ range .Highlights }}
    \item {{ . }}
{{ end }}
\end{itemize}
{{ end }}

\vspace{0.1cm}
{{ end }}
{{ end }}

\end{document}
